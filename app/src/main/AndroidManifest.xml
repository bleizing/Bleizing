<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bleizing.bleizing">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UnitTesting.Activity.UnitTestingActivity" />
        <activity android:name=".DataWebServer.Activity.DataWebServerActivity" />
        <activity android:name=".GPS.Activity.GPSActivity" />
        <activity android:name=".Information.Activity.InformationActivity"></activity>
    </application>

</manifest>